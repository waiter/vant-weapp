<wxs src="../wxs/utils.wxs" module="utils" />

<view class="van-stepper custom-class">
  <view
    class="{{ utils.bem('stepper__minus', { disabled: minusDisabled }, 'minus-class') }}"
    hover-class="van-stepper__minus--hover"
    hover-stay-time="70"
    bind:tap="onMinus"
  />
  <input
    type="{{ integer ? 'number' : 'digit' }}"
    class="{{ utils.bem('stepper__input', { disabled: disabled || disableInput }, 'input-class') }}"
    style="{{ inputWidth ? 'width: ' + inputWidth : '' }}"
    value="{{ value }}"
    focus="{{ focus }}"
    disabled="{{ disabled || disableInput }}"
    bindinput="onInput"
    bind:focus="onFocus"
    bind:blur="onBlur"
  />
  <view
    class="{{ utils.bem('stepper__plus', { disabled: plusDisabled }, 'plus-class') }}"
    hover-class="van-stepper__plus--hover"
    hover-stay-time="70"
    bind:tap="onPlus"
  />
</view>
