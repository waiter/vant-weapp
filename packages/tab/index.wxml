<wxs src="../wxs/utils.wxs" module="utils" />

<view
  wx:if="{{ animated || inited }}"
  class="{{ utils.bem('tab__pane', { active, inactive: !active }, 'custom-class') }}"
  style="{{ animated || active ? '' : 'display: none;' }} {{ width ? 'width:' + width + 'px;' : '' }}"
>
  <slot />
</view>
