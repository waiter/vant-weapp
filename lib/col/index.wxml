<wxs src="../wxs/utils.wxs" module="utils" />

<view
  class="{{ utils.bem('col', [span], offset ? 'custom-class van-col--offset-' + offset : 'custom-class') }} "
  style="{{ viewStyle }}"
>
  <slot />
</view>
