<wxs src="../wxs/utils.wxs" module="utils" />

<view
  class="{{ utils.bem('tabbar', { fixed, safe: isIPhoneX && safeAreaInsetBottom }, border ? 'custom-class van-hairline--top-bottom' : 'custom-class') }}"
  style="{{ zIndex ? 'z-index: ' + zIndex : '' }}"
>
  <slot />
</view>
