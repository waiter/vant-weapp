<wxs src="../wxs/utils.wxs" module="utils" />

<view
  class=" {{ utils.bem('tag', [size, { mark, plain, round }], plain ? 'custom-class van-hairline--surround' : 'custom-class') }}"
  style="{{ viewStyle }}"
>
  <slot />
</view>
